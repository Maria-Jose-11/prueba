import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'chat-with-images',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
